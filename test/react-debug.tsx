import React from "react";

const Test = () => {
  return (
    <div style={{ color: "#007acc" }}>
      {title}
      {children}
      {config.label}
      {props.title}
      {item.description}
    </div>
  );
};
